# AI 일기 분석 백엔드 - Firebase Admin SDK 중심 의존성
# Railway 배포 최적화 버전 - JWT 라이브러리 제거

# ============================================
# 핵심 웹 프레임워크 (필수)
# ============================================
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0

# ============================================
# 데이터베이스 (PostgreSQL) - 선택사항
# ============================================
sqlalchemy==2.0.23
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9

# ============================================
# AI 분석 서비스 (핵심)
# ============================================
google-generativeai==0.3.2

# ============================================
# 인증 & 보안 (Firebase Admin SDK만)
# ============================================
firebase-admin==6.4.0
passlib[bcrypt]==1.7.4

# ============================================
# 기본 유틸리티 (필수)
# ============================================
python-dotenv==1.0.0
httpx==0.25.2
email-validator==2.1.0

# ============================================
# 로깅 & 모니터링
# ============================================
structlog==23.2.0

# ============================================
# 캐싱 (선택사항 - Redis)
# ============================================
redis==4.5.4

# ============================================
# 타입 검증 & 호환성
# ============================================
typing-extensions==4.8.0

# ============================================
# 개발/테스트 도구 (로컬 개발용)
# ============================================
pytest==7.4.3
pytest-asyncio==0.21.1

# ============================================
# 제거된 패키지들 
# ============================================
# python-jose[cryptography]==3.3.0  # 제거: Firebase Admin SDK만 사용
# numpy==1.24.3                     # 제거: 용량 최적화
# scipy==1.11.4                     # 제거: 용량 최적화
# nltk==3.8.1                       # 제거: 용량 최적화
# spacy==3.7.2                      # 제거: 용량 최적화
# textblob==0.17.1                  # 제거: 용량 최적화

# ============================================
# 핵심 변경사항
# ============================================
# ✅ python-jose 완전 제거 
# ✅ Firebase Admin SDK만 사용
# ✅ JWT 토큰 검증은 Firebase에서 제공하는 기능 활용
# ✅ 의존성 개수 대폭 감소: 16개 패키지
# ✅ 예상 이미지 크기: 800MB-1GB (기존 대비 80% 감소)
# ✅ 빌드 시간: 3-4분 (기존 대비 70% 감소)
