# AI 일기 분석 백엔드 - 최적화된 의존성
# Flutter 앱 연동 및 Railway 배포 최적화 버전

# ============================================
# 핵심 웹 프레임워크 (필수)
# ============================================
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0

# ============================================
# 데이터베이스 (PostgreSQL)
# ============================================
sqlalchemy==2.0.23
alembic==1.13.1
asyncpg==0.29.0
# psycopg2-binary는 Railway에서 빌드 문제가 있을 수 있으므로 asyncpg 사용

# ============================================
# AI 분석 서비스 (핵심)
# ============================================
google-generativeai==0.3.2

# ============================================
# 인증 & 보안 (Firebase 포함)
# ============================================
firebase-admin==6.4.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ============================================
# 기본 유틸리티 (필수)
# ============================================
python-dotenv==1.0.0
httpx==0.25.2
email-validator==2.1.0

# ============================================
# 로깅 & 모니터링
# ============================================
structlog==23.2.0

# ============================================
# 캐싱 (선택사항 - Redis)
# ============================================
redis==5.0.1

# ============================================
# 타입 검증 & 호환성
# ============================================
typing-extensions==4.8.0

# ============================================
# 개발/테스트 도구 (로컬 개발용)
# ============================================
# 프로덕션 배포시에는 이 섹션을 주석 처리하거나 별도 requirements-dev.txt로 분리
pytest==7.4.3
pytest-asyncio==0.21.1

# ============================================
# 제거된 패키지들 (용량 최적화)
# ============================================
# sentence-transformers==2.2.2  # 500MB+ (큰 ML 모델)
# numpy==1.24.3                 # 대용량 수치 계산 라이브러리  
# scipy==1.11.4                 # 대용량 과학 계산 라이브러리
# nltk==3.8.1                   # 대용량 자연어처리 데이터
# spacy==3.7.2                  # 대용량 NLP 모델
# textblob==0.17.1              # 자연어처리 라이브러리
# aioredis==2.0.1               # redis로 통합
# celery[redis]==5.3.4          # 백그라운드 작업 (필요시 추가)
# sentry-sdk[fastapi]==1.38.0   # 모니터링 (필요시 추가)
# black==23.11.0                # 코드 포맷터 (개발용)
# flake8==6.1.0                 # 린터 (개발용)
# mypy==1.7.1                   # 타입 체커 (개발용)
# types-redis==4.6.0.11         # 타입 힌트 (개발용)
# psycopg2-binary==2.9.9        # asyncpg로 대체

# ============================================
# 추가 정보
# ============================================
# 총 패키지 수: 16개 (기존 30+ 개에서 50% 감소)
# 예상 이미지 크기: 800MB-1.2GB (기존 6.7GB에서 80% 감소)
# 빌드 시간: 3-5분 (기존 10-15분에서 70% 감소)
#
# 필요시 추가할 수 있는 패키지들:
# - sentry-sdk[fastapi]: 에러 모니터링
# - celery[redis]: 백그라운드 작업
# - numpy, scipy: 고급 수치 계산 필요시
# - sentence-transformers: 벡터 임베딩 필요시
