# 핵심 웹 프레임워크
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0

# 데이터베이스 (PostgreSQL만)
sqlalchemy==2.0.23
alembic==1.13.1
asyncpg==0.29.0

# Redis (경량화된 버전)
redis==5.0.1

# AI 서비스 (Google Gemini만 - 가장 경량)
google-generativeai==0.3.2

# 인증 (필수만)
python-jose[cryptography]==3.3.0

# 기본 유틸리티
python-dotenv==1.0.0
httpx==0.25.2
email-validator==2.1.0

# 로깅
structlog==23.2.0

# 타입 검증
typing-extensions==4.8.0
